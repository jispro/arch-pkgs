post_install() {
    post_upgrade

    echo "DONE with jispro-base package"
}

post_upgrade() {
    ln -sf /usr/share/zoneinfo/Europe/Amsterdam "/etc/localtime"
    echo "LANG=en_US.UTF-8" > /etc/locale.conf
    sed 's/#en_US/en_US/' -i /etc/locale.gen
    locale-gen
}