post_install() {
    post_upgrade

    echo "DONE with jispro-workstation package"
}

post_upgrade() {
    ln -sf /run/systemd/resolve/resolv.conf /etc/resolv.conf
    cp /usr/share/jepma/etc/libinput-gestures.conf /etc/libinput-gestures.conf
}